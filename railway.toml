[build]
builder = "nixpacks"

[deploy]
startCommand = "node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# Railway 볼륨 마운트 (대시보드에서 /app/data 경로로 볼륨 추가 필요)
# 볼륨이 없으면 배포 때마다 DB가 초기화됨
[[volumes]]
mountPath = "/app/data"